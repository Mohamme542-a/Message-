# قرارات أمن Alpha Byte

## سياسة الاحتفاظ المختارة

اعتمد المنتج **الخيار ب**: يحتفظ الخادم بالرسائل والمرفقات المشفّرة مدة لا تتجاوز سبعة أيام. بعد ذلك يحذف صف الرسالة ومرجع المرفق من الخادم، سواء اختار المستخدم مهلة أقصر أم لم يختر مهلة. تحتفظ الأجهزة بالظروف المشفّرة التي سبق تنزيلها في مخزن محلي، وتزيلها عند انتهاء مؤقت الرسالة المحلي. لذلك لا تتاح رسالة لم تُنزَّل سابقًا لمزامنة جهاز جديد بعد الحذف الخادمي.

يعتمد تنظيف الخادم الحالي على حذف صفوف قاعدة البيانات ومراجع التخزين. لا تؤكد واجهة التخزين المتاحة حذف بايتات الكائن فعليًا من مزود S3؛ لذا لا يجوز الادعاء بالإتلاف الفيزيائي للبايتات إلى أن تُضاف عملية حذف موثقّة لدى المزود. لا يرسل التطبيق رابط تخزين مباشر للمرفق: يتحقق الخادم من عضوية المحادثة النشطة ثم يمرر بايتات ciphertext فقط.

## مهمة التنظيف الدورية

تعمل مهمة Heartbeat باسم `alpha-byte-weekly-retention` كل يوم أحد عند **03:00 UTC** على المسار الخادمي `/api/scheduled/alpha-byte-retention`. معرّفها التشغيلي هو `FLqLugd3pk6Z96H4S9XHxu` ويُستخدم عند الفحص أو الإيقاف أو التعديل؛ المهمة تستدعي تنظيفًا idempotent لمراجع ciphertext والمرفقات المنتهية فقط.

## التشفير والحدود

تُشفَّر الرسالة أو المرفق على جهاز المرسل قبل الإرسال، ولا يتلقى الخادم إلا ظرفًا مشفّرًا وبيانات لازمة للتسليم والمهلة. يتحقق الخادم من أن جهاز الإرسال المسجل يتبع الحساب وأن رفع المرفق مرتبط بعضوية محادثة نشطة وبالمستخدم الرافع قبل قبول ظرف الرسالة. لا يكفي هذا وحده لادعاء تكافؤ مع بروتوكول مراسلة مدقق مثل Signal؛ يتطلب ذلك مراجعة مستقلة لإدارة المفاتيح، تبادل المفاتيح، دورانها، واستردادها.

المفاتيح ونسخ الرسائل المشفّرة الحالية مخزنة في Web Crypto وIndexedDB ضمن WebView. هذا لا يكافئ تخزين مفتاح خاص في Android Keystore، ولا ينفذ prekeys موقعة أو X3DH أو ratchet متعدد الأجهزة. يبقى ذلك حدًا معروفًا يجب معالجته قبل وصف المنتج بأنه بروتوكول E2EE مدقق للإطلاق الحساس.

## سلامة التطبيق والاستجابة للعبث

تُربط واجهات Android بحساب وشهادة خادم محددين، مع رفض الاتصالات النصية غير المشفرة. يجب تضمين مفتاح تثبيت احتياطي قبل تعطيل التطبيق بسبب تدوير شهادة الخادم.[1]

تستخدم نسخة Android الحالية تثبيت SPKI لمفتاح شهادة النطاق ومفتاح شهادة وسيطة احتياطي. يجب تحديث التطبيق قبل تغيير جهة إصدار الشهادة أو مفاتيحها؛ لأن تثبيت الشهادات ليس وسيلة لإخفاء اسم واجهة الخادم، بل لتقييد الشهادات المقبولة لذلك النطاق.

تتحقق النسخة الحالية من بصمة شهادة توقيع الإصدار عند ضبط خاصية البناء `ALPHA_RELEASE_CERT_SHA256`، وتلغي جلسة الخادم وتحذف رمز الجلسة المحلي إذا فشل الفحص. يتطلب بناء الإصدار توقيعًا فعليًا وبصمة الشهادة الصحيحة؛ أما بناء التصحيح فلا يقدّم ضمان الإصدار ذاته. لا يمنع ذلك أدوات root أو hook أو تجاوز TLS على جهاز معدل بالكامل، لكنه يضيف حاجزًا للتحقق المبكر وربطًا بخادم يرفض الجلسة عند الإخفاق.

تم بناء إصدار محلي موقّع والتحقق من أن بصمة شهادة APK تساوي بصمة شهادة الإصدار التالية: `2b64df4e1a80c4276d44e587839e3b71f2a6130c80b4e024f8d08013e3344c9d`. لم يُحفظ ملف التوقيع أو ملف `signing.properties` أو كلمات المرور داخل المصدر أو GitHub. تعذر إعداد أسرار GitHub Actions عبر الربط الحالي لأن واجهة GitHub أعادت خطأ صلاحية `403`؛ يبقى APK الموقّع المحلي هو نسخة الإصدار المتحقق منها حاليًا.

بعد بناء الإصدار، أزيل ملف التوقيع المرفوع وجميع ملفات خصائص التوقيع المؤقتة من مساحة العمل، وتحقق فحص APK من عدم احتواء الحزمة على ملف keystore أو ملف خصائص توقيع. يجب تدوير كلمات مرور ملف التوقيع قبل أي بناء مستقبلي إذا ظهرت في سجل تنفيذ أو قناة غير مخصصة لإدارة الأسرار.

عند إتاحة التطبيق عبر Google Play، يضاف تحقق خادمي من حكم سلامة التطبيق في نقاط الوصول الحساسة. لا يُستخدم الحكم كإشارة وحيدة؛ بل تدعم الاستجابة المتدرجة: السماح، السماح بقيود، أو إبطال جلسة/جهاز ومنع الواجهة الخادمية. لا يتضمن النظام قطع الإنترنت الفعلي عن جهاز المستخدم.[2]

## المراجع

[1]: https://developer.android.com/privacy-and-security/security-config "Android Network Security Configuration"
[2]: https://developer.android.com/google/play/integrity/overview "Google Play Integrity API overview"
