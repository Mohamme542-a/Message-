# تطبيقات Android الأصلية

توجد حزمتان منفصلتان لتطبيقَي Android. يستخدم **Alpha Byte User** واجهة WebView محلية تُحمّل ملفات التصميم المضمنة داخل APK فقط، ولا تفتح رابطًا خارجيًا. أما **Alpha Byte Admin** فيبقى بواجهة Android محلية.

| الحزمة | المعرّف | الوظيفة |
|---|---|---|
| Alpha Byte User | `com.qarfash.abuser` | حزمة WebView محلية تضم تفعيل الحساب، إنشاء الحساب أو تسجيل الدخول، البحث عن حسابات جاهزة للمراسلة، المحادثات، الإعدادات والجلسة. |
| Alpha Byte Admin | `com.qarfash.abadmin` | دخول إدارة مستقل، الصيانة، التسجيل، الإشعارات، التحديث الإلزامي، إجراءات الحسابات، البلاغات، وموافقات امتيازات الحساب. |

لا يوجد في الحزمتين أي مفتاح Firebase أو Cloudinary أو رمز تفعيل أو رمز إدارة. تتصلان بالخادم عبر HTTPS للبيانات والتحقق فقط.

## خريطة واجهة Alpha Byte User المضمنة

تستخدم نسخة **Alpha Byte User** واجهة Alpha Byte الكاملة ضمن ملفات HTML وCSS وJavaScript محلية داخل APK. لا تُفتح صفحة مستضافة ولا يُحمّل محتوى الواجهة من رابط خارجي؛ والاتصال الخارجي محصور في HTTPS لواجهات Alpha Byte الخاصة بالتحقق والحساب والمراسلة.

| شاشة الموقع/التدفق | المقابل الأصلي في AB User | حالة البيانات |
|---|---|---|
| بوابة التفعيل | إدخال رمز التفعيل والتحقق الخادمي مع تنبيه ضمن الشاشة | فعلي عبر HTTPS |
| الدخول وإنشاء الحساب | تبويبان محليان لاسم المستخدم والعبارة السرية | فعلي عبر HTTPS |
| المحادثات | إنشاء محادثة، إرسال نص أو مرفق مشفّر محليًا، تفاعلات، ومدد حذف | فعلي عبر HTTPS عند وجود عضوين سجلا جهازين نشطين |
| الأشخاص | بحث حي باسم المستخدم يعرض الحسابات الجاهزة للمراسلة وزر مراسلة مستقل | فعلي عبر HTTPS؛ لا تُنشأ جهات وهمية |
| الملف الشخصي | اسم المستخدم ومعرّف الحساب الراجعان من الجلسة | فعلي من الجلسة |
| الخصوصية | إعداد إيصالات القراءة وتوضيح حدود بيانات الحساب | محلي للجهاز |
| الأجهزة | وصف صريح للجلسة الحالية على هذا الجهاز | فعلي للجهاز الحالي |
| الإعدادات | اسم المستخدم وعبارة الدخول وقفل الرسائل المحلي والاستعادة والجلسات وإيصالات القراءة | محلي وخادمي بحسب الإجراء |

> لا تعرض المحادثات أو الأشخاص محتوى افتراضيًا. لا تُنشأ رسائل وهمية أو جهات وهمية؛ وتُضاف القوائم والتفاصيل فقط عند وصول بيانات حية من الخادم.

## الأذونات

يطلب تطبيق المستخدم إذن `INTERNET` للاتصال الآمن، ويطلب إذن `CAMERA` **فقط عند اختيار زر التقاط صورة**. عند فتح منتقي مرفق، يطلب إذن قراءة الصور المحدود المتاح في Android (`READ_MEDIA_IMAGES` في Android 13 فأعلى، و`READ_EXTERNAL_STORAGE` في الإصدارات الأقدم) عند الحاجة فقط؛ أما الملفات العامة فتُختار من منتقي ملفات النظام ولا يحصل التطبيق على إذن تخزين شامل. لا يطلب التطبيق الميكروفون أو جهات الاتصال أو الموقع أو إذن إشعارات لأن هذه الوظائف غير منفذة حاليًا.

## مفتاح استعادة المحادثات وقفل الرسائل

ينشئ المستخدم مفتاح استعادة خاصًا من الإعدادات ويعرضه التطبيق **مرة واحدة فقط** للنسخ والحفظ خارج التطبيق. لا يُرسل المفتاح ولا يُخزن كنص صريح؛ يشتق التطبيق منه محليًا مفتاح تشفير عبر PBKDF2 ثم يرفع للخادم نسخة مبهمة مشفّرة من مفاتيح المحادثات المتاحة على الجهاز. عند تغيير الهاتف أو مسح بياناته، يسجل المستخدم الدخول ثم يدخل مفتاحه لاسترداد مفاتيح المحادثات من النسخة المشفّرة.

> لا تعيد الاستعادة ذاكرة المرفقات المحلية أو هوية الجهاز، ولا تعيد محتوى انتهت فترة احتفاظه لدى الخادم؛ يحتفظ الخادم بظروف الرسائل ومراجع المرفقات لمدة لا تتجاوز أسبوعًا. لا يستطيع الخادم فك النسخة أو معرفة مفتاح الاستعادة.

يوفر خيار **رمز الرسائل** قفلًا محليًا للتطبيق على الجهاز الحالي فقط. هذا الرمز ليس عبارة دخول الحساب ولا مفتاح استعادة، ولا ينبغي اعتباره بديلًا عن تخزين مفاتيح عتادي موثوق أو حماية نظام الجهاز.

يستطيع المستخدم اختيار **حذف من هذا الجهاز** من أدوات المحادثة. يحذف ذلك مفتاح المحادثة والنسخ المؤقتة من حساب الجهاز الحالي ويخفي المحادثة محليًا؛ ولا يحذف رسائل أعضاء آخرين أو النسخ الخادمية المؤقتة.

## روابط المجموعات والقنوات

ينشئ المالك أو المشرف رابط دعوة بصيغة `alphabyte://join?invite=…`. عند فتحه على Android، يستقبل التطبيق الرمز بعد التحقق من شكله ويفتح Alpha Byte؛ عند توفر جلسة الحساب يرسل طلب انضمام مضبوطًا للخادم. لا يحمل الرابط عنوان المحادثة أو رسائلها أو مفتاحها. يبقى الطلب معلقًا حتى يقبله مالك أو مشرف يغلّف مفتاح المحادثة لجهاز العضو المقبول.

تستخدم روابط `https` كمسار توافق، لكن فتح التطبيق بشكل موثوق يعتمد على الرابط المخصص المذكور أعلاه ما لم يضف ناشر التطبيق ملف Digital Asset Links صالحًا للنطاق.

## توقيع إصدارك

يوجد إعداد توقيع إصدار خارجي في كل مشروع Android. عند توفير ملف keystore الخاص بك بأمان وكلمة مرور المخزن والاسم المستعار وكلمة مروره، يُنشأ ملف `signing.properties` محليًا فقط ولا يُضاف إلى Git أو إلى ملف APK. بعد ذلك يكون أمر البناء:

```bash
./gradlew assembleRelease
```

ويُنتج APK موقّعًا تحت `app/build/outputs/apk/release/`.
