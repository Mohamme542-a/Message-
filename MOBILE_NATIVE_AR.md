# تطبيقات Android الأصلية

توجد حزمتان منفصلتان لتطبيقَي Android. يستخدم **AB User** واجهة WebView محلية تُحمّل ملفات التصميم المضمنة داخل APK فقط، ولا تفتح رابطًا خارجيًا. أما **AB Admin** فيبقى بواجهة Android محلية.

| الحزمة | المعرّف | الوظيفة |
|---|---|---|
| AB User | `com.qarfash.abuser` | حزمة WebView محلية تضم تفعيل الحساب، إنشاء الحساب أو تسجيل الدخول، التنقل والإعدادات والجلسة. |
| AB Admin | `com.qarfash.abadmin` | دخول إدارة مستقل، الصيانة، التسجيل، الإشعارات، التحديث الإلزامي، إجراءات الحسابات، البلاغات، والتدقيق. |

لا يوجد في الحزمتين أي مفتاح Firebase أو Cloudinary أو رمز تفعيل أو رمز إدارة. تتصلان بالخادم عبر HTTPS للبيانات والتحقق فقط.

## خريطة واجهة AB User المضمنة

تستخدم نسخة **AB User** واجهة AB الكاملة ضمن ملفات HTML وCSS وJavaScript محلية داخل APK، مع نظام أسود ومساحات وبطاقات وحدود مستوحاة من واجهة الموقع. لا تُفتح صفحة مستضافة ولا يُحمّل محتوى الواجهة من رابط خارجي؛ والاتصال الخارجي محصور في HTTPS لواجهات AB الخاصة بالتحقق والحساب.

| شاشة الموقع/التدفق | المقابل الأصلي في AB User | حالة البيانات |
|---|---|---|
| بوابة التفعيل | إدخال رمز التفعيل والتحقق الخادمي مع تنبيه ضمن الشاشة | فعلي عبر HTTPS |
| الدخول وإنشاء الحساب | تبويبان محليان لاسم المستخدم والعبارة السرية | فعلي عبر HTTPS |
| المحادثات | عنوان AB وحالة فارغة هادئة عند عدم وجود محادثات | صادقة؛ لا تُنشأ رسائل وهمية |
| الأشخاص | شاشة جهات وطلبات بحالة فارغة عند غياب سجلات حية | صادقة؛ لا تُنشأ جهات وهمية |
| الملف الشخصي | اسم المستخدم ومعرّف الحساب الراجعان من الجلسة | فعلي من الجلسة |
| الخصوصية | إعداد إيصالات القراءة وتوضيح حدود بيانات الحساب | محلي للجهاز |
| الأجهزة | وصف صريح للجلسة الحالية على هذا الجهاز | فعلي للجهاز الحالي |
| الإعدادات | ملف شخصي وخصوصية وأجهزة وإيصالات قراءة وتسجيل خروج | محلي وخادمي بحسب الإجراء |

> لا تعرض المحادثات أو الأشخاص محتوى افتراضيًا. ستُضاف القوائم والتفاصيل فقط عند ربط واجهات خادمية حية لها؛ وبذلك لا يوحي التطبيق بوجود بيانات أو رسائل غير موجودة.

## الأذونات

لا يطلب التطبيقان إلا إذن `INTERNET` لأن الوظائف الحالية تتصل بخادم AB. لم نضف الكاميرا أو الميكروفون أو جهات الاتصال أو الموقع أو التخزين أو الإشعارات، لأنها غير منفذة في النسخة الحالية.

## توقيع إصدارك

يوجد إعداد توقيع إصدار خارجي في كل مشروع Android. عند توفير ملف keystore الخاص بك بأمان وكلمة مرور المخزن والاسم المستعار وكلمة مروره، يُنشأ ملف `signing.properties` محليًا فقط ولا يُضاف إلى Git أو إلى ملف APK. بعد ذلك يكون أمر البناء:

```bash
./gradlew assembleRelease
```

ويُنتج APK موقّعًا تحت `app/build/outputs/apk/release/`.
